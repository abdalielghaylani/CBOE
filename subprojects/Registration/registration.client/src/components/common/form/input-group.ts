import {
  Component,
  Input
} from '@angular/core';
import { FormControl } from '@angular/forms';

@Component({
  selector: 'reg-input-group',
  template: `<ng-content></ng-content>`
})
export class RegInputGroup {
};
